import Foundation

// Copy this file to Secrets.swift and fill in your actual values.
// Secrets.swift is gitignored to keep your keys private.

enum Secrets {
    // Firebase Cloud Functions base URL (RECOMMENDED - keeps API key secure)
    // Deploy functions first, then set this to your functions URL
    // Example: "https://us-central1-your-project-id.cloudfunctions.net"
    static let firebaseFunctionsBaseURL = ""

    // Legacy: Direct Claude API key (only used if firebaseFunctionsBaseURL is empty)
    // NOT RECOMMENDED for production - API key can be extracted from app binary
    static let claudeAPIKey = "your-claude-api-key-here"
}
